compiler = g++

outputPath = ./
outputName = build
output = -o $(outputPath)$(outputName)

libs = -lSDL2

sourcesPath = ../src/
sources = $(sourcesPath)main.cpp


all:
	$(compiler) $(sources) $(libs) $(output)
